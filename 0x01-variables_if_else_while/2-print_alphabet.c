#include <stdio.h>

/**
 * main - prints alpha in lowercaase
 * Return: Always 0
 */
int main(void)
{
	char alp;

	for (lc =  'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
